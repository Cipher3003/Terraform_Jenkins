pipeline {
    agent any

    stages {
        stage('Webhook Test') {
            steps {
                echo 'Webhook triggered Jenkins build started successfully.'
            }
        }
    }
}
